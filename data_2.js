let price = 19.90;
let discount = 0.4;

console.log(price * (1 - discount)); // final price after discount

let priceWithDiscount = price * (1 - discount);

console.log(priceWithDiscount);

const name = "Notebook";
const category = "Stationery";
console.log("Product:" + name + " is " + priceWithDiscount + " dollars. From Category: " + category);